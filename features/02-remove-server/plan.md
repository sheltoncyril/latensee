# Remove Server

Allow users to delete servers from the list (builtin or custom).

## Changes
- `dns_benchmark.py`: add Remove button per checkbox row or right-click context menu
- Remove from `self.servers` and `self._checkboxes` lists
