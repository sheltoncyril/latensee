# Benchmark History

Save runs to JSON, load previous run to compare on chart.

## Changes
- `dns_benchmark.py`: save each run to `~/.latensee/history.json` on completion
- Add "History" button opening a dialog to select a past run
- Chart shows previous run as faded bars alongside current run
